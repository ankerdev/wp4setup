import React from 'react';
import { hot } from 'react-hot-loader';

const Home = () => (
  <main className="main flex--down">
    <h1>Webpack 4 setup by 🇳🇴</h1>
  </main>
);

export default hot(module)(Home);
